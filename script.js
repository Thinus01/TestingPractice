function stringLength(string) {
    let totalLength = 0;
    for (let i = 0; i <= string.length; i++) {
        totalLength = i;
    }
    console.log(totalLength);
    return totalLength;
}
module.exports = stringLength("appel")